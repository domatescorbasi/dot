" autocmd vimenter * NERDTree
set clipboard=unnamedplus " what's yanked is put into X's + buffer (mouse
			" middle click buffer is * instead of +, if you want to
			" use that as main buffer)
set paste               " Paste from a windows or from vim
set go+=a               " Visual selection automatically copied to clipboard
